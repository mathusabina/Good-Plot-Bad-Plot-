# Use coord_cartesian() instead of scale_y_break() to avoid issues
coord_cartesian(ylim = c(500, 4500)) +
theme_minimal() +
theme(legend.position = "none")
# Display the combined plot
combined_plot
# Load necessary libraries
library(dplyr)
library(ggplot2)
library(ggbreak)  # For truncated y-axis (optional)
# Prepare the data from the Seatbelts dataset
deaths_all <- as.data.frame(Seatbelts)
# Seatbelts has 192 rows: Jan 1969 to Dec 1984
deaths_all$Year  <- rep(1969:1984, each = 12)
deaths_all$Month <- rep(1:12, times = 16)
# Create a period variable: "Pre-1983" and "Post-1983"
deaths_all <- deaths_all %>%
mutate(period = ifelse(Year < 1983, "Pre-1983", "Post-1983"))
# Create a new variable 'casualties' by summing drivers, front, and rear fatalities
deaths_all <- deaths_all %>%
mutate(casualties = drivers + front + rear)
# (1) Aggregate data for the bar chart: Calculate average casualties per period
agg_data <- deaths_all %>%
group_by(period) %>%
summarise(avg_casualties = mean(casualties), .groups = "drop")
# (2) Create the combined plot with overlapping layers:
combined_plot <- ggplot() +
# Layer 1: Aggregated Bar Chart
geom_bar(data = agg_data,
aes(x = period, y = avg_casualties, fill = period),
stat = "identity", color = "black", alpha = 0.6) +
# Layer 2: Boxplot for individual casualties
geom_boxplot(data = deaths_all,
aes(x = period, y = casualties, fill = period),
width = 0.3, alpha = 0.4) +
# Layer 3: Scatter Plot with jitter for individual data points
geom_point(data = deaths_all,
aes(x = period, y = casualties, color = period),
position = position_jitter(width = 0.4), size = 2) +
# Labels and Title
labs(title = "Combined Plot of Road Casualties Pre- and Post-1983",
x = "Period",
y = "Casualties") +
# Manual color scales
scale_fill_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
scale_color_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
# Use coord_cartesian() instead of scale_y_break() to avoid issues
coord_cartesian(ylim = c(500, 4500)) +
theme_minimal() +
theme(legend.position = "none")
# Display the combined plot
combined_plot
# (2) Create the combined plot with overlapping layers:
combined_plot <- ggplot() +
# Layer 1: Aggregated Bar Chart
geom_bar(data = agg_data,
aes(x = period, y = avg_casualties, fill = period),
stat = "identity", color = "black", alpha = 0.6) +
# Layer 2: Boxplot for individual casualties
geom_boxplot(data = deaths_all,
aes(x = period, y = casualties, fill = period),
width = 0.3, alpha = 0.4) +
# Layer 3: Scatter Plot with jitter for individual data points
geom_point(data = deaths_all,
aes(x = period, y = casualties, color = period),
position = position_jitter(width = 0.4), size = 2, alpha(0.4)) +
# Labels and Title
labs(title = "Combined Plot of Road Casualties Pre- and Post-1983",
x = "Period",
y = "Casualties") +
# Manual color scales
scale_fill_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
scale_color_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
# Use coord_cartesian() instead of scale_y_break() to avoid issues
coord_cartesian(ylim = c(500, 4500)) +
theme_minimal() +
theme(legend.position = "none")
# Display the combined plot
combined_plot
# Load necessary libraries
library(dplyr)
library(ggplot2)
library(ggbreak)  # For truncated y-axis (optional)
# Prepare the data from the Seatbelts dataset
deaths_all <- as.data.frame(Seatbelts)
# Seatbelts has 192 rows: Jan 1969 to Dec 1984
deaths_all$Year  <- rep(1969:1984, each = 12)
deaths_all$Month <- rep(1:12, times = 16)
# Create a period variable: "Pre-1983" and "Post-1983"
deaths_all <- deaths_all %>%
mutate(period = ifelse(Year < 1983, "Pre-1983", "Post-1983"))
# Create a new variable 'casualties' by summing drivers, front, and rear fatalities
deaths_all <- deaths_all %>%
mutate(casualties = drivers + front + rear)
# (1) Aggregate data for the bar chart: Calculate average casualties per period
agg_data <- deaths_all %>%
group_by(period) %>%
summarise(avg_casualties = mean(casualties), .groups = "drop")
# (2) Create the combined plot with overlapping layers:
combined_plot <- ggplot() +
# Layer 1: Aggregated Bar Chart
geom_bar(data = agg_data,
aes(x = period, y = avg_casualties, fill = period),
stat = "identity", color = "black", alpha = 0.6) +
# Layer 2: Boxplot for individual casualties
geom_boxplot(data = deaths_all,
aes(x = period, y = casualties, fill = period),
width = 0.3, alpha = 0.4) +
# Layer 3: Scatter Plot with jitter for individual data points
geom_point(data = deaths_all,
aes(x = period, y = casualties, color = period),
position = position_jitter(width = 0.4), size = 2, alpha(0.4)) +
# Labels and Title
labs(title = "Combined Plot of Road Casualties Pre- and Post-1983",
x = "Period",
y = "Casualties") +
# Manual color scales
scale_fill_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
scale_color_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
# Use coord_cartesian() instead of scale_y_break() to avoid issues
coord_cartesian(ylim = c(500, 4500)) +
theme_minimal() +
theme(legend.position = "none")
# Load necessary libraries
library(dplyr)
library(ggplot2)
library(ggbreak)  # For truncated y-axis (optional)
# Prepare the data from the Seatbelts dataset
deaths_all <- as.data.frame(Seatbelts)
# Seatbelts has 192 rows: Jan 1969 to Dec 1984
deaths_all$Year  <- rep(1969:1984, each = 12)
deaths_all$Month <- rep(1:12, times = 16)
# Create a period variable: "Pre-1983" and "Post-1983"
deaths_all <- deaths_all %>%
mutate(period = ifelse(Year < 1983, "Pre-1983", "Post-1983"))
# Create a new variable 'casualties' by summing drivers, front, and rear fatalities
deaths_all <- deaths_all %>%
mutate(casualties = drivers + front + rear)
# (1) Aggregate data for the bar chart: Calculate average casualties per period
agg_data <- deaths_all %>%
group_by(period) %>%
summarise(avg_casualties = mean(casualties), .groups = "drop")
# (2) Create the combined plot with overlapping layers:
combined_plot <- ggplot() +
# Layer 1: Aggregated Bar Chart
geom_bar(data = agg_data,
aes(x = period, y = avg_casualties, fill = period),
stat = "identity", color = "black", alpha = 0.6) +
# Layer 2: Boxplot for individual casualties
geom_boxplot(data = deaths_all,
aes(x = period, y = casualties, fill = period),
width = 0.3, alpha = 0.4) +
# Layer 3: Scatter Plot with jitter for individual data points
geom_point(data = deaths_all,
aes(x = period, y = casualties, color = period),
position = position_jitter(width = 0.4), size = 2, alpha 0.4)) +
# Load necessary libraries
library(dplyr)
library(ggplot2)
library(ggbreak)  # For truncated y-axis (optional)
# Prepare the data from the Seatbelts dataset
deaths_all <- as.data.frame(Seatbelts)
# Seatbelts has 192 rows: Jan 1969 to Dec 1984
deaths_all$Year  <- rep(1969:1984, each = 12)
deaths_all$Month <- rep(1:12, times = 16)
# Create a period variable: "Pre-1983" and "Post-1983"
deaths_all <- deaths_all %>%
mutate(period = ifelse(Year < 1983, "Pre-1983", "Post-1983"))
# Create a new variable 'casualties' by summing drivers, front, and rear fatalities
deaths_all <- deaths_all %>%
mutate(casualties = drivers + front + rear)
# (1) Aggregate data for the bar chart: Calculate average casualties per period
agg_data <- deaths_all %>%
group_by(period) %>%
summarise(avg_casualties = mean(casualties), .groups = "drop")
# (2) Create the combined plot with overlapping layers:
combined_plot <- ggplot() +
# Layer 1: Aggregated Bar Chart
geom_bar(data = agg_data,
aes(x = period, y = avg_casualties, fill = period),
stat = "identity", color = "black", alpha = 0.6) +
# Layer 2: Boxplot for individual casualties
geom_boxplot(data = deaths_all,
aes(x = period, y = casualties, fill = period),
width = 0.3, alpha = 0.4) +
# Layer 3: Scatter Plot with jitter for individual data points
geom_point(data = deaths_all,
aes(x = period, y = casualties, color = period),
position = position_jitter(width = 0.4), size = 2, alpha 0.4)+
# Display the combined plot
combined_plot
# Load necessary libraries
library(dplyr)
library(ggplot2)
library(ggbreak)  # For truncated y-axis (optional)
# Prepare the data from the Seatbelts dataset
deaths_all <- as.data.frame(Seatbelts)
# Seatbelts has 192 rows: Jan 1969 to Dec 1984
deaths_all$Year  <- rep(1969:1984, each = 12)
deaths_all$Month <- rep(1:12, times = 16)
# Create a period variable: "Pre-1983" and "Post-1983"
deaths_all <- deaths_all %>%
mutate(period = ifelse(Year < 1983, "Pre-1983", "Post-1983"))
# Create a new variable 'casualties' by summing drivers, front, and rear fatalities
deaths_all <- deaths_all %>%
mutate(casualties = drivers + front + rear)
# (1) Aggregate data for the bar chart: Calculate average casualties per period
agg_data <- deaths_all %>%
group_by(period) %>%
summarise(avg_casualties = mean(casualties), .groups = "drop")
# (2) Create the combined plot with overlapping layers:
combined_plot <- ggplot() +
# Layer 1: Aggregated Bar Chart
geom_bar(data = agg_data,
aes(x = period, y = avg_casualties, fill = period),
stat = "identity", color = "black", alpha = 0.6) +
# Layer 2: Boxplot for individual casualties
geom_boxplot(data = deaths_all,
aes(x = period, y = casualties, fill = period),
width = 0.3, alpha = 0.4) +
# Layer 3: Scatter Plot with jitter for individual data points
geom_point(data = deaths_all,
aes(x = period, y = casualties, color = period),
position = position_jitter(width = 0.4), size = 2, alpha = 0.3)+
# Labels and Title
labs(title = "Combined Plot of Road Casualties Pre- and Post-1983",
x = "Period",
y = "Casualties") +
# Manual color scales
scale_fill_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
scale_color_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
# Use coord_cartesian() instead of scale_y_break() to avoid issues
coord_cartesian(ylim = c(500, 4500)) +
theme_minimal() +
theme(legend.position = "none")
# Display the combined plot
combined_plot
# (2) Create the combined plot with overlapping layers:
combined_plot <- ggplot() +
# Layer 1: Aggregated Bar Chart
geom_bar(data = agg_data,
aes(x = period, y = avg_casualties, fill = period),
stat = "identity", color = "black", alpha = 0.6) +
# Layer 2: Boxplot for individual casualties
geom_boxplot(data = deaths_all,
aes(x = period, y = casualties, fill = period),
width = 0.3, alpha = 0.4) +
# Layer 3: Scatter Plot with jitter for individual data points
geom_point(data = deaths_all,
aes(x = period, y = casualties, color = period),
position = position_jitter(width = 0.4), size = 2, alpha = 0.7)+
# Labels and Title
labs(title = "Combined Plot of Road Casualties Pre- and Post-1983",
x = "Period",
y = "Casualties") +
# Manual color scales
scale_fill_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
scale_color_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
# Use coord_cartesian() instead of scale_y_break() to avoid issues
coord_cartesian(ylim = c(500, 4500)) +
theme_minimal() +
theme(legend.position = "none")
# Display the combined plot
combined_plot
# Load necessary libraries
library(dplyr)
library(ggplot2)
library(ggbreak)  # For truncated y-axis (optional)
# Prepare the data from the Seatbelts dataset
deaths_all <- as.data.frame(Seatbelts)
# Seatbelts has 192 rows: Jan 1969 to Dec 1984
deaths_all$Year  <- rep(1969:1984, each = 12)
deaths_all$Month <- rep(1:12, times = 16)
# Create a period variable: "Pre-1983" and "Post-1983"
deaths_all <- deaths_all %>%
mutate(period = ifelse(Year < 1983, "Pre-1983", "Post-1983"))
# Create a new variable 'casualties' by summing drivers, front, and rear fatalities
deaths_all <- deaths_all %>%
mutate(casualties = drivers + front + rear)
# (1) Aggregate data for the bar chart: Calculate average casualties per period
agg_data <- deaths_all %>%
group_by(period) %>%
summarise(avg_casualties = mean(casualties), .groups = "drop")
# (2) Create the combined plot with overlapping layers:
combined_plot <- ggplot() +
# Layer 1: Aggregated Bar Chart
geom_bar(data = agg_data,
aes(x = period, y = avg_casualties, fill = period),
stat = "identity", color = "black", alpha = 0.6) +
# Layer 2: Boxplot for individual casualties
geom_boxplot(data = deaths_all,
aes(x = period, y = casualties, fill = period),
width = 0.3, alpha = 0.4) +
# Layer 3: Scatter Plot with jitter for individual data points
geom_point(data = deaths_all,
aes(x = period, y = casualties, color = period),
position = position_jitter(width = 0.4), size = 2, alpha = 0.7)+
# Labels and Title
labs(title = "Road Casualties Post- and Pre-1983",
x = "Period",
y = "Casualties") +
# Manual color scales
scale_fill_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
scale_color_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
# Use coord_cartesian() instead of scale_y_break() to avoid issues
coord_cartesian(ylim = c(500, 4500)) +
theme_minimal() +
theme(legend.position = "top")
# Display the combined plot
combined_plot
# Load necessary libraries
library(dplyr)
library(ggplot2)
library(ggbreak)  # For truncated y-axis (optional)
# Prepare the data from the Seatbelts dataset
deaths_all <- as.data.frame(Seatbelts)
# Seatbelts has 192 rows: Jan 1969 to Dec 1984
deaths_all$Year  <- rep(1969:1984, each = 12)
deaths_all$Month <- rep(1:12, times = 16)
# Create a period variable: "Pre-1983" and "Post-1983"
deaths_all <- deaths_all %>%
mutate(period = ifelse(Year < 1983, "Pre-1983", "Post-1983"))
# Create a new variable 'casualties' by summing drivers, front, and rear fatalities
deaths_all <- deaths_all %>%
mutate(casualties = drivers + front + rear)
# (1) Aggregate data for the bar chart: Calculate average casualties per period
agg_data <- deaths_all %>%
group_by(period) %>%
summarise(avg_casualties = mean(casualties), .groups = "drop")
# (2) Create the combined plot with overlapping layers:
combined_plot <- ggplot() +
# Layer 1: Aggregated Bar Chart
geom_bar(data = agg_data,
aes(x = period, y = avg_casualties, fill = period),
stat = "identity", color = "black", alpha = 0.6) +
# Layer 2: Boxplot for individual casualties
geom_boxplot(data = deaths_all,
aes(x = period, y = casualties, fill = period),
width = 0.3, alpha = 0.4) +
# Layer 3: Scatter Plot with jitter for individual data points
geom_point(data = deaths_all,
aes(x = period, y = casualties, color = period),
position = position_jitter(width = 0.4), size = 2, alpha = 0.7)+
# Labels and Title
labs(title = "Road Casualties Post- and Pre-1983",
x = "Period",
y = "Casualties") +
# Manual color scales
scale_fill_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
scale_color_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
# Use coord_cartesian() instead of scale_y_break() to avoid issues
coord_cartesian(ylim = c(500, 4500)) +
theme_minimal() +
theme(legend.position = "none")
# Display the combined plot
combined_plot
# Create a period variable: "Pre-1983" and "Post-1983"
deaths_all <- deaths_all %>%
mutate(period = ifelse(Year < 1983, "Pre-1983", "Post-1983"))
# Create a new variable 'casualties' by summing drivers, front, and rear fatalities
deaths_all <- deaths_all %>%
mutate(casualties = drivers + front + rear)
# (1) Aggregate data for the bar chart: Calculate average casualties per period
agg_data <- deaths_all %>%
group_by(period) %>%
summarise(avg_casualties = mean(casualties), .groups = "drop")
# (2) Create the combined plot with overlapping layers:
combined_plot <- ggplot() +
# Layer 1: Aggregated Bar Chart
geom_bar(data = agg_data,
aes(x = period, y = avg_casualties, fill = period),
stat = "identity", color = "black", alpha = 0.6) +
# Layer 2: Boxplot for individual casualties
geom_boxplot(data = deaths_all,
aes(x = period, y = casualties, fill = period),
width = 0.3, alpha = 0.4) +
# Layer 3: Scatter Plot with jitter for individual data points
geom_point(data = deaths_all,
aes(x = period, y = casualties, color = period),
position = position_jitter(width = 0.4), size = 2, alpha = 0.7)+
# Labels and Title
labs(title = "Road Casualties Post- and Pre-1983",
x = "Period",
y = "Casualties") +
# Manual color scales
scale_fill_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
scale_color_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
# Use coord_cartesian() instead of scale_y_break() to avoid issues
coord_cartesian(ylim = c(500, 4500)) +
theme_minimal() +
theme(legend.position = "none")
# Display the combined plot
combined_plot
# Create a period column for grouping
deaths_all <- deaths_all %>%
mutate(period = ifelse(Year < 1983, "Pre-1983", "Post-1983"))
# Create a new variable 'casualties' by summing drivers, front, and rear fatalities
deaths_all <- deaths_all %>%
mutate(casualties = drivers + front + rear)
View(deaths_all)
View(combined_plot)
agg_data <- deaths_all %>%
group_by(period) %>%
summarise(avg_casualties = mean(casualties), .groups = "drop")
View(agg_data)
#Install packages
install.packages("ggplot2")
install.packages("tidyr")
install.packages("dplyr")
install.packages("ggsci")
install.packages("ggbreak")
install.packages("patchwork")
#Loading packages
library(ggplot2)
library(tidyr)
library(dplyr)
library(ggsci)
library(ggbreak)
library(patchwork)
data("Seatbelts")
# Overview
View(Seatbelts)
?Seatbelts
colnames(Seatbelts)
nrow(Seatbelts)
# The columns "drivers", "fornt" and "rear" describe the amount of people killed or seriously injured while sitting in these positions. The amount of rows represent the monthly data from January 1969 to December 1984. On 31 January 1983 the compulsory wearing of seat belts was introduced. The change in casualities will be visualized withe the following plots.
#Adding a column for Month and selecting the
deaths_all<- as.data.frame(Seatbelts)
deaths_all$Year <- rep(1969:1984, each = 12)
deaths_all$Month <- rep(1:12, times = 16)
deaths_all$Date <- as.Date(paste(deaths_all$Year, deaths_all$Month, "01", sep = "-"), format = "%Y-%m-%d")
deaths<- select(deaths_all,Date,drivers,front,rear)
deaths_long <- deaths %>%
pivot_longer(cols = c(drivers,front, rear),
names_to = "seat_position",
values_to = "deaths")
ggplot(deaths_long, aes(x = Date, y = deaths, fill = seat_position)) +
geom_density(stat = "identity",position = "stack", alpha = 0.7, linewidth = 0.2) +
scale_x_date(date_labels = "%Y", date_breaks = "1 year") +
labs(title = "Fatalities by Seat Position: Stacked Density Plot",
x = "Year",
y = "Number of Fatalities",
fill = "Seat Position") +
scale_fill_lancet() +
theme_minimal() +
theme(
axis.text.x = element_text(angle = 45, hjust = 1),
# Main grid
panel.grid.major = element_line(color = "grey70", linewidth = 0.4),
#  Secondary grid
panel.grid.minor = element_line(color = "grey70", linewidth = 0.3))
#change date to 1976-1985 to zoom in on the time of change 1983
deaths_long2 <- deaths %>%
filter(Date >= as.Date("1976-01-01") & Date <= as.Date("1985-12-31")) %>%
pivot_longer(cols = c(drivers,front, rear),
names_to = "seat_position",
values_to = "deaths")
ggplot(deaths_long2, aes(x = Date, y = deaths, fill = seat_position)) +
geom_density(stat = "identity",position = "stack", alpha = 0.7, linewidth = 0.2) +
scale_x_date(date_labels = "%Y", date_breaks = "1 year") +  #
labs(title = "Fatalities by Seat Position: Stacked Density Plot",
x = "Year",
y = "Number of Fatalities",
fill = "Seat Position") +
scale_fill_lancet() +
theme_minimal() +
theme(
axis.text.x = element_text(angle = 45, hjust = 1),
# Main grid
panel.grid.major = element_line(color = "grey70", linewidth = 0.4),
#  Secondary grid
panel.grid.minor = element_line(color = "grey70", linewidth = 0.3))
# Create a period column for grouping
deaths_all <- deaths_all %>%
mutate(period = ifelse(Year < 1983, "Pre-1983", "Post-1983"))
# Create a new variable "casualties" by summing drivers, front, and rear casualties
deaths_all <- deaths_all %>%
mutate(casualties = drivers + front + rear)
# Calculate average casualties per period
agg_data <- deaths_all %>%
group_by(period) %>%
summarise(avg_casualties = mean(casualties), .groups = "drop")
# Create the combined plot with overlapping layers:
combined_plot <- ggplot() +
# Layer 1: Aggregated Bar Chart
geom_bar(data = agg_data,
aes(x = period, y = avg_casualties, fill = period),
stat = "identity", color = "black", alpha = 0.6) +
# Layer 2: Boxplot for individual casualties
geom_boxplot(data = deaths_all,
aes(x = period, y = casualties, fill = period),
width = 0.3, alpha = 0.4) +
# Layer 3: Scatter Plot with jitter for individual data points
geom_point(data = deaths_all,
aes(x = period, y = casualties, color = period),
position = position_jitter(width = 0.4), size = 2, alpha = 0.7)+
# Labels and title
labs(title = "Road Casualties Post- and Pre-1983",
x = "Period",
y = "Casualties") +
# Color scales
scale_fill_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
scale_color_manual(values = c("Pre-1983" = "#e69f00", "Post-1983" = "#56B4E9")) +
coord_cartesian(ylim = c(500, 4500)) +
theme_minimal() +
theme(legend.position = "none")
# Display the combined plot
combined_plot
install.packages("patchwork")
install.packages("ggsci")
install.packages("ggplot2")
install.packages("dplyr")
